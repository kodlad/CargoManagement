<Window x:Class="CargoManagement.AddDriverWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить водителя"
        Height="250" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center">ФИО:</TextBlock>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="NameBox" Margin="4"/>

        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center">ВУ №:</TextBlock>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="LicenseBox" Margin="4"/>

        <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center">Телефон:</TextBlock>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="PhoneBox" Margin="4"/>

        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Width="75" Margin="0,0,8,0" Click="Save_Click">Сохранить</Button>
            <Button Width="75" Click="Cancel_Click">Отмена</Button>
        </StackPanel>
    </Grid>
</Window>
